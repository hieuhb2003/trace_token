# GPU Configuration
GPU_ID=0

# Port Configuration (Single GPU)
API_PORT=9000

# Port Configuration (Multi GPU)
API_PORT_GPU0=9000
API_PORT_GPU1=9001

# Langfuse Cloud Configuration (Free tier)
LANGFUSE_PUBLIC_KEY=your_langfuse_public_key
LANGFUSE_SECRET_KEY=your_langfuse_secret_key
LANGFUSE_HOST=https://cloud.langfuse.com

# Project Configuration (Single GPU)
PROJECT_NAME=my-project

# Project Configuration (Multi GPU)
PROJECT_NAME_GPU0=project-gpu0
PROJECT_NAME_GPU1=project-gpu1

# Model Configuration (Optional)
MODEL_NAME=Qwen/Qwen2.5-7B-Instruct
GPU_MEMORY_UTILIZATION=0.7

# HuggingFace Token (Optional - for private models)
HF_TOKEN=your_hf_token_here 